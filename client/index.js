import ReactDOM from 'react-dom';
import React from 'react';
require('./index.html')

ReactDOM.render(
   <h1>A simple react app with webpack-dev-servers that auto reloads on changes</h1>,
  document.getElementById('root')
);
